{
  "name": "app-builder-super-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node src/app_state.test.js && node src/frontend_structure.test.js && node src/browser_flow.test.js && node src/engine.test.js",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "axe-core": "^4.13.0",
    "jsdom": "^30.0.1",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
